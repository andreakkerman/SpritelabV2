{
  "name": "spritelab-v2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve": "python3 -m http.server 4173",
    "check": "node --check src/app.js && node --check src/model.js && node --check src/renderer.js && node --check src/storage.js && python3 tools/check_frontend.py",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "jszip": "^3.10.1",
    "pngjs": "^7.0.0"
  }
}
